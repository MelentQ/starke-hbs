<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, viewport-fit=cover">
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="format-detection" content="telephone=no">
    <link rel="stylesheet" href="css/styles.min.css" />

    <meta name="description" content="Работаем так, чтобы Вам не пришлось сомневаться в выборе" />
    <meta name="keywords" content="Работаем так, чтобы Вам не пришлось сомневаться в выборе" />

    <meta property="og:title" content="Работаем так, чтобы Вам не пришлось сомневаться в выборе" />
    <meta property="og:description" content="Работаем так, чтобы Вам не пришлось сомневаться в выборе" />
    <meta property="og:image" content="img/content/gallery/gallery-big-1.jpg" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://example.com" />
    <meta property="og:locale" content="ru_RU" />

    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon">

    {{!-- Подключение Яндекс.Картам --}}
    {{#if hasMap}}
    <script src="https://api-maps.yandex.ru/2.1/?apikey=4af5bbbe-a25a-4591-8bd3-670c28bb10a6&lang=ru_RU" type="text/javascript"></script>
    {{/if}}
    <title>Starke | {{ title }}</title>
</head>

<body class="no-touch" ontouchstart="">
    <div class="sprite" aria-hidden="true" style="position: absolute; width: 0; height: 0; overflow: hidden;">
        {{> sprite }}
    </div>

    <div class="page-content">
        {{> header }}

        <main class="page-main{{#if headerIsOver}} page-main--with-header{{/if}}">
            <img class="page__bg-image" src="img/header-bg-brick.png" alt="Фоновое изображение">
            <div class="container">
                <div class="page__title-block {{#if banner}}page__title-block--with-banner{{/if}}">
                    {{!-- Подключаем фоновое изображение --}}
                    {{#if banner}}
                    <picture>
                        <source srcset="img/content/banners/{{banner.webp}}" type="image/webp">
                        <img class="page__banner" src="img/content/banners/{{banner.image}}" alt="{{title}}">
                    </picture>
                    <div class="page__banner-overlay"></div>
                    {{/if}}
                    {{!-- Подключение хлебных крошек --}}
                    {{#if breadcrumbs}}
                    {{> breadcrumbs links=breadcrumbs}}
                    {{/if}}

                    {{#if hasTitle}}
                    {{!-- Если есть h1 в начале страницы --}}
                    <h1 class="page__main-title">{{title}}</h1>
                    {{/if}}
                </div>
            </div>
            
            {{#block "main"}}{{/block}}

            {{!-- Подключение формы --}}
            {{#if hasForm}}
                {{> form-question text="У Вас остались вопросы?"}}
            {{/if}}
        </main>
        {{> footer }}
        {{> ie-placeholder }}
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="js/app.js"></script>
        <script src="assets/js/backend.js"></script>
    </div>
</body>

</html>